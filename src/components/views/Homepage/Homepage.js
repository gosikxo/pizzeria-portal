import React from 'react';
import { component } from './Homepage.module.scss';

function Homepage() {
  return (
    <div className={component}>
      <h1>Homepage</h1>
    </div>
  );
}

Homepage.propTypes = {

};

export default Homepage;

import React from 'react';
import { component } from './Homepage.scss';

function Homepage() {
  return (
    <div className={component}>
      Homepage
    </div>
  );
}

Homepage.propTypes = {

};

export default Homepage;
